<div class="library">
    <div class="library_title">
        {{!-- Will this require a title from my backend? --}}
        <h2 class='libTitle'>Library</h2>
    </div>

    <div class="addPlaylist">
        <a style="display: flex; align-items: center;" href="/spotify-auth"><span
                style="font-size:36px; color:#f5f5f5;">Add a Playlist</span><span class="material-icons"
                style="font-size: 90px; color: #f5f5f5;">add</span></a>
    </div>

    <div class="playlists_groups">
        {{#each playlist}}
        <div>
            <h2>{{playlist_name}}</h2>
            <img src={{playlist_artwork}} />
        </div>
        {{/each}}
    </div>
</div>

<script>
</script>

{{!-- {{#each id}}
<h2>{{playlist_name}}</h2>
<img src={{image_artwork}} />
{{/each}} --}}